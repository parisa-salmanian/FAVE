/**
 * road-network.js — Roads now rendered by MapLibre GL base map.
 * Stub class kept so view.js imports don't break.
 */
export class RoadNetwork {
  build()              {}
  dispose()            {}
  setAppearanceMode()  {}
}
