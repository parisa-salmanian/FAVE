# --- Python ---
__pycache__/
*.pyc
.venv/
venv/

# --- Editor / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Node ---
node_modules/

# --- Runtime / cache / job output (per-run; never commit) ---
api_data/
frontend/assets/data/cache/
# OSMnx/Overpass bake cache (top-level, ~450 MB hash-named responses; regenerable).
cache/

# --- EpiCity raw source data (bake-time only, regenerable from the EpiCity
# checkout; ~700 MB). The runtime app loads only the BAKED products
# (transit/network.json, demographics/deso.geojson, access2sfca/*), which ARE
# committed. Re-bake with tools/bake_*.py if you need to regenerate them. ---
frontend/assets/data/epicity/
frontend/assets/data/cities/*/epicity/

# --- Large data files: tracked via Git LFS, not ignored. ---
# (see .gitattributes for the LFS pattern list.)
# These two are tooling-only and not needed at runtime, so still ignored:
frontend/assets/data/sweden_regso.geojson
frontend/assets/data/all_sweden_regso.geojson
frontend/assets/data/DeSO_2025.gpkg

# --- Backups (local-only, never commit) ---
*.bak
*.geojson.bak

# --- Lantmäteriet downloads (local-only; ~3-4 GB for all of Sweden) ---
# Run tools/fetch_lantmateriet_buildings.py to populate this folder.
lantmateriet/

# --- OSRM graph cache (local-only; ~12 GB, files exceed GitHub's 100 MB limit) ---
# Regeneratable build cache for baking routing matrices. Re-download/rebuild via
# the OSRM bake setup; the baked matrices live under cities/*/routing instead.
tools/osrm/

# --- Secrets (NEVER commit) ---
.env
.lantmateriet_credentials

# Local Playwright verify screenshots (debug artifacts)
tools/_verify_*.png
